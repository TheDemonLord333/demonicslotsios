{
  "name": "demonic-slots-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Optional online sync backend for the Demonic Slots iOS app: username registration and Soul Coin balance sync, plus an admin API/UI to override a player's balance.",
  "main": "src/server.js",
  "type": "commonjs",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "dependencies": {
    "better-sqlite3": "^11.3.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
